{
  // =====================================================================
  // 武器需求框架 (WRF) - 弹药重量范围 → 力量需求映射表
  // =====================================================================
  // 此文件仅在 MCM 中选择【弹药重量】模式时生效。
  // 弹药重量四舍五入后按优先级匹配，第一个命中即为最终需求。
  // =====================================================================
  "Enabled": true,
  "Priority": 0,

  "Rules": [
    // 0 ~ 1 磅：极轻弹药（无需求）
    { "Priority": 50, "Value": 0, "Conditions": { "MinWeight": 0, "MaxWeight": 1 } },

    // 1 ~ 2 磅：轻型弹药（需求 1）
    { "Priority": 49, "Value": 1, "Conditions": { "MinWeight": 1, "MaxWeight": 2 } },

    // 2 磅以上：封顶 2
    { "Priority": 48, "Value": 2, "Conditions": { "MinWeight": 2 } }
  ]
}
